spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:tcp://localhost/~/istp
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true


